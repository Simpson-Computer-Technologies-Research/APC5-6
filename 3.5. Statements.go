package main

// Import fmt package
import "fmt"

// Main function
func main() {

	// If 1 is equal to 1
	if 1 == 1 {
		fmt.Println(1)

		// If the above is not true, then else if
	} else if 1 != 1 {
		fmt.Println("Not 1")

		// Else if none of the above are true, then else
	} else {

	}
}
